//! TEST: tuple_grouping_not_tuple
//! EXIT: 42

// Single expression in parentheses without trailing comma is NOT a tuple
// (42) is just 42, not a tuple

pub fn main() i32 {
    let x = (42)
    return x
}
